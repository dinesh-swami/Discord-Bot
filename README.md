🤖 Discord Bot — URL Shortener

A simple Discord bot that generates short URLs using a command. Built with Node.js and Discord.js.

🚀 Features
Slash command support (/create)
Message command support (create <url>)
Generates short URL (logic extendable)
Uses environment variables for security
Clean and minimal setup
